# Copy this file to .env for local configuration if desired.
# The current settings module reads environment variables directly and does
# not require python-dotenv.
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=replace-this-for-any-non-development-deployment
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
DJANGO_TIME_ZONE=Asia/Dhaka
